# Non-secret production values — safe to commit.
# Secrets (DATABASE_URI, PAYLOAD_SECRET) are injected by amplify.yml
# from Amplify Console branch environment variables at build time.
NODE_ENV=production
